<h1>Register</h1>
 
<% form_for :user, @user, :url=>users_path do |f| %>
  <%= f.error_messages %>
   <%= f.select :type, ["Athlete", "Coach"] %><br />
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Register" %>
<% end %>