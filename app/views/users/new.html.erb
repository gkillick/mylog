<h1>Register</h1>
 
<% form_for :user, @user, :url=>users_path do |f| %>
  <%= form.select :type, ["Athlete", "Coach"] %><br />
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Register" %>
<% end %>